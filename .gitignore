# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
/build
next-env.d.ts

# local SQLite database (lives on a Railway volume in prod)
/data/

# env files (keep .env.example)
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude Code local settings
.claude/

# Owner's personal spreadsheet — real habit data, not used by the app.
# Remove this line if you intentionally want it in the repo.
Routine.xlsx
.playwright-mcp/
